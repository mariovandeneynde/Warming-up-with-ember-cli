<div class='row'>
      <div class='col-sm-9'>
        {{outlet}}
      </div>
      <div class='col-sm-3'>
        <div class='list-group'>
          {{#each contact in model}}
            {{#link-to 'contact' contact classNames='list-group-item'}}
              {{contact.name}}
            {{/link-to}}
          {{/each}}
        </div>
      </div>
    </div>
